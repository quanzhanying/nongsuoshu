<div class="index-banner">
  <div class="hero-title">
    <h1>浓缩就是精华</h1>
    <p>为进步而读书
    </p>
  </div>
</div>

<p id="notice"><%= notice %></p>


<div class="row">
  <div class="col-md-12 col-lg-12 book-bg">

  </div>
</div>


<h1>Books</h1>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.content %></td>
        <td><%= link_to 'Show', book %></td>
        <!-- <td><%#= link_to 'Edit', edit_book_path(book) %></td>
        <td><%#= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>
  <%= will_paginate @books %>
