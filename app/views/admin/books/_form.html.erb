<%= simple_form_for [:admin, @book] do |f| %>
  <%= f.input :title, :label =>"浓缩书名称" %>
  <%= f.input :content, :label =>"浓缩书内容" %>
  <% if ["new", "create"].include?(controller.action_name) %>
    <%= f.submit "创建", class: "btn btn-success" %>
  <% else %>
    <%= f.submit "更新", class: "btn btn-success" %>
  <% end %>
<% end %>
